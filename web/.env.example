# Point the client at your running backend. Leave unset for local dev — Vite proxies
# /books, /ingest, /tts, /voices, /media to http://127.0.0.1:8600 automatically.
# VITE_API_BASE=http://127.0.0.1:8600
